let n = 5;

for(let i=1; i<=n; i++) {
    let output = "";
    for(let j=i; j<=n; j++) {
        output +=" ";
    }
    for(let j=2; j<=(2*i); j++) {
      if(j == 2 || j == (2*i) || i == n) {
        output +="*";
      } else {
        output += " ";
      }
    }
    console.log(output);
}

Output -

     *
    * *
   *   *
  *     *
 *********
