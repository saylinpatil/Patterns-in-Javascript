let n = 5;
for (let i = 1; i <= n; i++) {
  let output = "";
  for (let j = 1; j <= n; j++) {
    if (i === Math.floor(n/2)+1 || j === Math.floor(n/2)+1) {
      output += "* ";
    } else {
      output += "  ";
    }
  }
  console.log(output);
}
